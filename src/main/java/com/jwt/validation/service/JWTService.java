package com.jwt.validation.service;
import com.jwt.validation.entity.JWT;
import com.jwt.validation.repository.JWTRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;

//@Service
public class JWTService {

//    @Autowired
//    private JWTRepository jwtRepository;

//    public ResponseEntity<JWT> addToken(JWT jwt){

//        jwtRepository.save(jwt);
//        return new ResponseEntity<>(jwt, HttpStatus.ACCEPTED);
//    }
}
